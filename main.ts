//Convert Minutes into Seconds
// Instruction: Write a function that takes an integer minutes and converts it to seconds.
function convert(minutes: number) {
    return minutes * 60
    }
    console.log(convert(5));
    